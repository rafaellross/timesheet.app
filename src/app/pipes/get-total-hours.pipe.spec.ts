import { GetTotalHoursPipe } from './get-total-hours.pipe';

describe('GetTotalHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTotalHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
