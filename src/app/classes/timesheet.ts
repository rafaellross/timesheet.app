import { Employee } from "./employee";

export class Timesheet {
    id: number;
    weekStart: string;    
    employee: number;
    start: number;
    end: number;
    interval: number;    
    dayNumber: Number;
}
