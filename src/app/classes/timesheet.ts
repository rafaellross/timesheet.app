import { Employee } from "./employee";

export class Timesheet {
    id: number;
    weekStart: string;
    dayNumber: Number;
    employee: number;
    start: number;
    end: number;
    interval: number;    
}
